# P21：BSW 通信对象图与变更影响

状态：planned，2026-09-24。P20 已由实现提交 `31ca4ef`、run `35979820300` 七 job 和本地双项目 SocketCAN 完整流程验收；本阶段尚未实现，不把 P20 的结果比较当作已有配置影响分析。

## 用户结果

给定同一项目两版配置，输出改变的通信对象、沿信号/PDU/路由的受影响范围，以及可能需要重跑的 P20 验收项；每条关系都有输入来源，无法证明的部分明确为 unknown。

## 下一次从这里开始

1. 审计 P20 两项目 DBC/intent/runtime binding 中已有字段，定义稳定对象 ID、对象类型、方向、来源 locator 与有类型的关系。首批只覆盖 ComSignal、IPdu、PduR route、CanIf PDU，不把命名后缀当作 AUTOSAR 语义证明。
2. 实现纯函数构图及闭合、版本化契约；保留现有 intent 与 P20 工作流兼容。支持两项目 Tx/Rx，重复身份、缺引用和无法解释的关系必须可定位。
3. 建立规则覆盖清单：断引用、重复身份、方向冲突、长度/布局冲突、路由端点错误、配置变化影响。每类都要有正负样例、规则依据和适用边界；属于平台自定义 intent 约束的规则必须明确标注，不能冒充 vendor ECUC 验证。
4. 两版图比较保留对象身份、来源差异、影响传播路径和未知断点，再与 P20 的向量/验收 ID 关联；仅凭不同项目同名对象不能比较。
5. 交付一条可操作 CLI/项目路径、真实正负例与可读报告；全量质量门、两平台公开场景、七 job 和实现提交/run 记录闭环后才冻结。

## 阶段门

- 两项目与 Tx/Rx 两链均可构图，来源可以复验。
- 六类规则均有正负例及来源；不支持语义不会静默通过。
- 两版输入可追踪到受影响对象、传播路径和需复验的验收项。
- P20 项目/review/compare、迁移及旧报告消费者保持通过，版本改变显式管理。
- 本地场景、远端七 job 和文档状态一起完成。个人 BSW 学习掌握单独验收。

主阶段保持 P21；ARXML/商业工具桥接和独立 ECU 生命周期分别属于后续 P22/P23，不用旁支替代当前对象图与影响分析。
